

 - Check if this is a static project (already has a .staticrc)
   - If not create one
 - Check if S3 bucket exists
  - If not create one
 - Check if static hosting is enabled
  - If not enable it
 - Delete all existing files in bucket
 - Upload files to S3
 - Setup
